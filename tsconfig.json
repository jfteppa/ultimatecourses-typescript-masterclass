{
  "compilerOptions": {
    "target": "es5",
    "lib": ["es6", "dom"],
    "module": "commonjs",
    "outDir": "dist",
    /*
      it will report module resolution loggin messages
      we can see where all of our modules are then coming from
    */
    // "traceResolution": true

    /*
      it gives us the amount of files that TS has had to look through
      it also tells how many lines of code, how many nodes, symbols,
      how much memory was used how long it took to emit our JS file 
      fro the TS file 
    */
    // "diagnostics": true

    /*
      to see what files were then processed 
    */
    // "listFiles": true

    /*
      it tells the name of the files that were generated
    */
    // "listEmittedFiles": true

    /*
      changes the way errors are presented 
    */
    "pretty": true
  }
}
